<h1>Users Show Page</h1>

<%= link_to "Create new hike", new_user_hike_path(@user) %>

<h3><%= @user.username %></h3> has created the following hikes:
<ul><% @user.hikes.each do |hike| %>
  <li><%= link_to hike.name, hike_path(hike) %></li>
<% end %></ul>

<h3>Click <%= link_to('here', '/hikes/completed') %> to see your completed hikes!</h3>
